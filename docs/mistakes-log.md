# Beluga Mistakes Log

작업 중 발생한 실패, 오개념, 설정 오류 및 디버깅 경험을 기록하여 재발을 방지한다.

## 원칙
1. 작업 시작 전 해당 영역의 기존 기록을 읽는다.
2. 새 오류 발생 시 아래 표에 행을 추가한다.

---

## 기록 표

| 날짜 | 영역 | 원인 / 현상 | 해결 / 예방책 |
|------|------|-------------|---------------|
| 2026-09-23 | gitops | identity 평문 preflight(test 11)가 존재하지만 CI가 호출하는 `make validate`에 연결되지 않아 이슈 #2 회귀 차단에서 빠져 있었음. 별도 조사에서 `beluga-*` 역할 별칭과 현재 KafkaUser/Keycloak 사용자 이름을 혼동하면 Kafka 기본 허용 정책의 analyst 쓰기 거부까지 제거됨을 OPA로 재현(현 정책 4/4 통과, 세 principal 규칙 삭제 사본 1/4 통과·3/4 실패) | test 11을 test 13·14와 같은 `make validate` 경로에 연결. 보안 검사 파일의 존재뿐 아니라 CI 호출 경로를 확인한다. 역할·그룹(`admins/engineers/analysts`)과 사용자(`beluga-admin/beluga-engineer/beluga-analyst`)를 구분하고, 별칭 삭제 전 실제 사용자 정의와 허용/거부 동작을 대조한다. Kafka 원본 정책과 기본 비활성 `opaAuthorizer`는 유지했으며 이 증거는 로컬 정적 검증에 한정됨. 같은 작업에서 test 14의 tsx IPC 차단 대비 재시도 경로가 npm workspace 재구성(beluga-manager `8614966`) 이전 경로 `bin/policyctl.ts`를 가리켜 제한된 샌드박스에서만 `ERR_MODULE_NOT_FOUND`로 실패함을 확인하고 `packages/policy-compiler/bin/policyctl.ts`로 정정(재시도 경로 단독 실행 exit 0, trino.rego 0 diff) |
| 2026-09-23 | ci | #103 오프라인 의존성 게이트 작성 중 정상 fixture의 줄 연결 문자열에 불필요한 문자가 들어가 먼저 실패했고, 이후 전체 파일 스캔이 로컬 `scripts/.DS_Store`를 UTF-8 소스로 읽다 실패했다. 검증 준비의 kubeconfig 추출도 `cannot locate context beluga`로 실패 | 정상 fixture 통과와 음성 fixture의 예상 진단을 함께 확인해 엉뚱한 실패를 변조 탐지로 오인하지 않는다. bytecode·OS 메타데이터만 명시적으로 제외하고 설치 명령의 파일 전체 예외는 두지 않는다. 클러스터 불필요 검증은 `KUBECONFIG=/dev/null`로 분리하며 라이브 검증으로 주장하지 않는다. 이후 `make validate`는 새 게이트를 통과했지만 기존 test 14가 인접 저장소의 `beluga-manager/bin/policyctl.ts` 누락으로 실패했다(`ERR_MODULE_NOT_FOUND`, make exit 2). 이 환경 의존 실패를 새 게이트 성공과 구분하고, 우회하거나 타 저장소를 수정하지 않는다 |
| 2026-09-23 | gitops | #47 정적 인벤토리 작업에서 기본 Helm 렌더의 서비스 Certificate 3개(APISIX/OpenLDAP/Trino)는 duration·renewBefore가 없고 CA는 renewBefore가 없음을 확인. 기존 렌더/lint만으로는 갱신 설정과 TLS Secret 연결 누락을 검출하지 못함 | 기본 수명·갱신 비율을 명시하고 make validate에 렌더 기반 인벤토리 게이트와 양성·음성 fixture 추가. TLS 참조→Certificate→Issuer를 네임스페이스별로 검사하고 inline 인증서 및 미분류 외부 Secret 참조는 차단한다. JSON은 매 실행 생성한다. 정적 통과는 실제 만료·회전·재로딩·CA 신뢰 재배포를 증명하지 않으며 #47 운영 수용 기준은 열어 둔다 |
| 2026-09-15 | gitops | OpenLDAP 보안 하드닝(`cb7ac7f`)의 정적 SAST 통과를 런타임 완료와 구분하고 수정 기록을 보완할 필요가 있었음. 기존 템플릿은 KSV-0014/KSV-0118 6건을 발생시켰음 | UID/GID 999, 권한 상승 금지, capabilities 제거, seccomp, 읽기 전용 루트와 서버의 `/var/lib/openldap/run`·`/tmp` emptyDir를 적용한 기존 변경을 재검증. `trivy config --severity HIGH,CRITICAL --exit-code 1 gitops/charts/beluga-platform/templates/openldap.yaml`은 0건·exit 0, `make lint`·`make validate`도 exit 0(2026-09-15). 이 증거는 정적 검사에 한정하며, 수정 후 독립 리뷰 최종 판정과 실클러스터 기동·인증 경로 검증은 아직 확보하지 못했다. 읽기 전용 루트 변경은 쓰기 경로·볼륨 권한과 실제 기동을 함께 검증해야 한다 |
| 2026-08-10 | docs | write_to_file 도구 호출 시 ArtifactMetadata 포함으로 인한 경로 오류 | 프로젝트 코드/문서 작성 시에는 ArtifactMetadata 파라미터 제외 |
| 2026-08-10 | lake | VERSIONS.md에 "Lakekeeper 0.7.0"으로 기록했으나 실제 배포 이미지는 `tabulario/iceberg-rest:latest` — 컴포넌트 자체가 다르고(D4 위반) 태그도 미고정. 문서와 배포가 소리 없이 갈라진 상태로 커밋됨 | VERSIONS.md를 실제 배포 기준으로 정정하고 D4 불일치를 §9 리스크·백로그에 명시. 예방: 컴포넌트 행 추가 시 values.yaml의 image 값과 대조하고, tests/에 VERSIONS.md↔values 드리프트 검증 추가 예정 |
| 2026-08-10 | k8s | 클린 인스톨 E2E 1라운드에서 "렌더 통과 ≠ 기동" 결함 8건 실측: ① CNPG webhook 준비 전 차트 적용(`sleep 10` + `\|\| true`가 실패를 삼켜 성공 배너) ② OPA가 ConfigMap 마운트의 `..data` 심링크까지 재귀 로드해 정책 3중 로드로 크래시 ③ OPA 기본 localhost 바인딩이라 Service/프로브 접근 불가 ④ OPA 프로브 경로 오기(`/v1/health`는 404 — 정상 서버를 liveness가 계속 살해) ⑤ APISIX가 conf 디렉터리에 인스턴스 상태를 쓰려다 읽기 전용 subPath 마운트에서 크래시 ⑥ APISIX `/apisix/status` 프로브는 데이터 플레인 포트에 없음(404) ⑦ OpenSearch에 `DISABLE_SECURITY_PLUGIN`+`plugins.security.disabled` 중복 설정으로 기동 거부 ⑧ PG 15+에서 `OWNER` 없는 `CREATE DATABASE`는 public 스키마 권한이 없어 전 메타 DB 소비자(Keycloak/Airflow/Lakekeeper/OM) 크래시 + migrate Job이 backoffLimit 소진으로 사망 | 수정: 오퍼레이터 `rollout status` 대기, OPA 파일 명시 로드+`--addr=0.0.0.0`+`/health` 프로브, APISIX initContainer로 쓰기 가능 conf 구성+tcpSocket 프로브, OpenSearch 단일 설정, `CREATE DATABASE ... OWNER` + 라이브 `ALTER OWNER` + Job 재생성. 교훈: helm 렌더·lint는 기동을 보증하지 않는다 — 신규 컴포넌트는 클린 인스톨 E2E 전에 "완료" 선언 금지, 부트스트랩의 `\|\| true`는 실패 은폐라 금지 |
| 2026-08-10 | ingest | Kafka 미배포가 4중 원인 사슬로 판명(테스트는 WARN만 내고 "전체 성공" — green gate 재발): ① Strimzi 릴리스 YAML의 RoleBinding이 기본 네임스페이스(myproject) 참조 → 오퍼레이터 lease 403으로 리더 선출 불가 ② Strimzi 0.45가 K8s 1.36(k3s 미고정 채널 설치 드리프트)과 비호환 → /version 감지 실패로 기동 중단 ③ 1.1.0 업그레이드 시 거대 CRD가 client-side apply 실패 + 구 v1beta2 storedVersions 충돌로 **조용히 미적용**(파이프 grep -c가 stderr를 삼킴) ④ 1.x는 CR이 v1 전용 + KafkaConnect의 group.id/스토리지 토픽이 일급 spec 필드로 변경 | 순서대로: sed 네임스페이스 치환, Strimzi 1.1.0+Kafka 4.3.0+Debezium 3.3.0 승급, CRD 전체 삭제 후 --server-side 신규 설치, 차트 apiVersion v1 승급 + Connect 스펙 필드화, k3s INSTALL_K3S_CHANNEL 고정, 테스트 WARN 6곳을 exit 1로 승격. 교훈: ⑴ 파이프라인에서 apply 결과를 grep으로 요약하면 실패가 숨는다 — 종료 코드/에러 스트림을 보존할 것 ⑵ 오퍼레이터 "Running"은 리컨실 성공이 아니다 — CR .status.conditions까지 검증 ⑶ 설치 스크립트의 미고정 채널은 시간이 지나면 반드시 드리프트 |
| 2026-08-10 | ingest | CDC 체인 후속 5건: ① Strimzi KafkaConnect CR에 debezium/connect 독립 이미지를 지정 — CR은 Strimzi 전용 런처(/opt/kafka/kafka_connect_run.sh)를 강제해 구조적 비호환(기동 즉사) ② Connect 기본 힙 -Xmx2G > 리밋 1536Mi로 OOMKilled ③ beluga_admin에 REPLICATION 속성 없어 WAL sender 거부 ④ Debezium 기본 publication.autocreate.mode=all_tables는 superuser 전용 ⑤ shop DB 스키마·시드 적용 메커니즘이 리포에 부재(빈 DB — 커넥터 등록 메커니즘도 함께 부재했음) | ① 독립 Deployment+Service+REST 멱등 등록 Job으로 전환(D6 수정) ② HEAP_OPTS 명시(§9 대응책 그대로) ③ postInit에 ALTER ROLE ... REPLICATION ④ filtered 모드(테이블 소유자 권한으로 충분) ⑤ 스키마를 차트 files/로 이동 + 시딩 Job. Keycloak도 KC25+ 헬스가 관리 포트(9000) 전용인데 8080 프로브로 정상 서버를 살해 — 프로브 포트 정정. 교훈: 파이프라인 "구성요소 존재"와 "말단까지 데이터 흐름"은 별개 — 검증은 토픽/행 수준까지 |
| 2026-08-10 | gitops | nginx Ingress → APISIX 전환 중 토큰 소진으로 중단 — 03-cni-metallb.sh에 ingress-nginx 설치가 잔존하고 Flink 라우트 누락 등 반쪽 전환 상태가 워킹트리에 방치됨 | 전환 마무리 커밋으로 정리 (nginx 설치 제거, Flink 라우트 추가). 예방: 아키텍처 전환은 시작 전에 대상 파일 목록을 만들어 한 커밋 단위로 완결하고, 중단 시 핸드오프에 미완료 목록을 남긴다 |
| 2026-08-11 | gitops | Keycloak LDAP 페더레이션이 3중 결함으로 막힘: ① 컴포넌트 존재 확인에 서버측 `?type=`/`?parent=&type=` 필터를 썼는데 **둘 다 빈 목록 반환** → 매 재시도마다 새로 생성해 프로바이더가 36개까지 중복, 그 중복이 사용자 검색을 400 `Cannot parse the JSON`으로 깨뜨려 D20 체인 전체가 마비 ② 동기화 엔드포인트 경로 오기(`/components/{id}/sync` → 404, 정답은 `/user-storage/{id}/sync`) ③ vegardit 이미지가 osixia와 다른 기본 트리(`ou=Users` 하위에 Internal/External/TechnicalAccounts + ou=Policies)를 제공하는데 `usersDn`은 평면 구조를 가정 | ①은 전체 목록을 받아 클라이언트에서 `providerId`로 거르도록 교정, ②는 경로 수정. ③은 미해소(다음 세션 1순위). 교훈: ⑴ **검증 명령에서 `2>/dev/null`로 오류를 숨기면 "빈 결과"와 "실패"를 구분할 수 없다** — 이번에 ldapsearch 오류를 숨겨 트리 구조 차이를 늦게 발견했다 ⑵ 멱등 Job은 "존재 확인이 실패하면 생성"이므로 확인 쿼리가 틀리면 무한 증식한다 — 생성 전에 개수 상한을 두거나 확인 쿼리를 먼저 단위 검증할 것 |
| 2026-08-11 | harness | 레인 T를 레인 R의 산출물(files/db-roles.sql) 확장 임무로 디스패치했으나 R이 아직 main에 병합되지 않아 T의 워크트리에는 그 파일이 없었음 — T가 같은 파일을 처음부터 재작성해 중복 발생. 결과적으로 T의 모델(NOLOGIN 권한 롤+상속+LDAP 로그인 분리)이 D19에 더 정합해 R을 대체 채택했으나, 순서 실수는 실수 | 다른 레인 산출물에 의존하는 레인은 **선병합 후 디스패치**가 원칙. 병렬이 필요하면 의존 파일을 프롬프트에 인라인으로 제공 |
| 2026-08-10 | harness | agy 워커 4개를 같은 워킹트리에서 병렬 실행 — 소유 파일을 분리했음에도 한 워커가 마무리 과정에서 리포 단위 revert를 수행해 다른 레인(04-lakekeeper.yaml)의 미커밋 수정이 소실됨. 오케스트레이터가 산출물을 먼저 읽어둔 덕에 컨텍스트에서 복원 | 파일을 수정하는 병렬 워커 레인은 D10 규칙대로 worktree 격리를 기본으로 하고, 동일 트리 병렬이 불가피하면 레인 완료 즉시 검수→스테이징(git add)으로 보호. 워커 프롬프트에 "git 되돌리기/checkout/restore 금지"를 명시 |
| 2026-08-10 | stream | 전 이미지 arm64 스윕(manifest inspect)에서 `apache/flink:1.20.0-scala_2.12-java17`이 amd64 전용으로 판명 — Docker 공식 `flink:` 리포와 ASF `apache/flink` 리포는 다른 리포이고 후자는 멀티아치 미제공. 원 구현 세션이 검증 없이 핀 | Docker 공식 `flink:1.20.0-scala_2.12-java17`(amd64+arm64)로 교체. 리포지토리 네임스페이스가 다르면 같은 태그라도 별개 이미지다 — 신뢰 리포 확인 포함해 manifest 게이트 적용 |
| 2026-08-10 | gitops | 고정 dev 자격증명 커밋(SET-AT-BOOTSTRAP, beluga-<client>-secret, admin/admin)을 "리포 관례"로 오판하고 보안 리뷰 3라운드에 걸쳐 수용 판정 — 실제 시리즈 관례(narwhal)는 설치 시 openssl rand 생성 + kubectl create secret + 조회 안내이며 고정값 커밋이 아님. 사용자 지적으로 발견 | D15로 정책 등재 후 부트스트랩 랜덤 생성으로 전면 전환. 예방: "관례"를 근거로 쓸 때는 참조 프로젝트(narwhal/kubemetal)에서 해당 관례를 실제로 확인한 뒤 인용 — 현 리포의 기존 코드는 관례의 증거가 아니다(그 코드 자체가 위반일 수 있음) |
| 2026-08-10 | harness | agy 워커의 이미지 태그 "검증 완료" 주장 2건이 허위로 판명 — `openpolicyagent/opa:1.1.0`은 arm64 미제공(파드 기동 시 exec format error 났을 것), `quay.io/lakekeeper/catalog:v0.14.0`은 태그 자체가 실존하지 않음(레인 보고서는 CHANGELOG 근거로 확인했다고 주장). 커밋 후 보안 리뷰 후속 검증 중 발견 | opa는 `1.1.0-static`(멀티아치), lakekeeper는 실존 확인된 `v0.13.1`로 정정. 예방: 신규/변경 이미지는 커밋 전 오케스트레이터가 `docker manifest inspect`로 태그 실존 + arm64 포함을 직접 재검증하는 것을 게이트로 — 워커의 "확인했다"는 증거가 아니다 (Never fabricate state의 워커 버전) |
| 2026-08-11 | gitops | D20이 OpenLDAP을 "계정 저장소"로 규정하고 Keycloak WRITABLE 페더레이션으로 런타임에 사용자를 만들게 해놓고, 정작 `/var/lib/ldap`·`/etc/ldap/slapd.d`가 **`emptyDir`** — 파드 재시작마다 런타임 생성 계정이 전멸하는 구조였음(시딩 Job이 복원하는 건 초기 5개 엔트리뿐). Deployment에 readiness/liveness 프로브도 전무 | PVC 2종(data 2Gi / config 1Gi) + `strategy: Recreate`(RWO라 RollingUpdate 불가) + `ldapwhoami` exec 프로브로 전환. 교훈: **설계가 "저장소"라고 부르는 컴포넌트는 볼륨 종류를 설계 검토 항목으로 올릴 것** — helm 렌더도 파드 기동도 통과하므로 E2E로도 드러나지 않고, 재시작이라는 정상 이벤트에서만 발현한다 |
| 2026-08-11 | gitops | 이미지 교체(osixia 1.5.0 → vegardit 2.6.10) 1차 산출물이 **새 이미지의 기본 시드 데이터를 확인하지 않음**. vegardit은 첫 기동에 데모 계정 3종(`employee1`/`guest1`/`machine1`)과 `groupOfUniqueNames` 그룹 4종을 심는데, Keycloak WRITABLE 페더레이션이면 그 유령 계정이 실제 플랫폼 사용자로 노출되고 D18의 "그룹 3종 단일 주체 축"이 오염됨. 워커 보고서는 렌더 성공만 근거로 완료를 주장했고, 오케스트레이터가 이미지의 `init_org_entries.ldif`를 직접 받아보고 발견 | ConfigMap을 `subPath`로 `/opt/ldifs/init_org_entries.ldif`에 덮어써 시드 억제. 교훈: 이미지 교체 게이트에 기존 항목(태그 실존 + arm64 manifest) 외에 **"기본 시드/샘플 데이터와 그 objectClass 확인"**을 추가한다 — 특히 인증·계정 저장소는 샘플 계정이 곧 취약점이다. 부수 교훈: 워커가 근거로 댄 변수(`LDAP_INIT_ADMIN_GROUP_DN`)가 실제로는 `run.sh`에 존재하지 않았음 — 변수의 "존재"와 "효과"는 별개다 |
| 2026-08-11 | harness | 웹 조사 서브에이전트가 **출처 URL까지 붙여 보고한 사실 2건이 허위**: ① `symas/openldap`은 Docker Hub에 존재하지 않음(404 — 실제 Symas 리포는 `symascorp/symas-openldap`이고 amd64 전용이라 애초에 arm64 게이트 탈락) ② 아카이브된 `jp-gouin/helm-openldap`의 기본 이미지를 bitnami로 보고했으나 실제 `values.yaml` 기본값은 메인테이너 포크 `jpgouin/openldap`. 두 건 모두 오케스트레이터가 그 보고를 근거로 사용자에게 권고를 냈다가 실조회로 뒤집음 | 레지스트리 API 실조회로 전 후보 재검증 후 권고 정정. 2026-08-10 "agy 워커 이미지 태그 허위 검증" 행과 **같은 유형의 재발이며, 그때 등재한 게이트가 이번엔 실제로 작동해 커밋 전에 잡았다**. 교훈: 그 게이트를 이미지뿐 아니라 **웹 조사 레인 전반으로 확대** — 출처 URL 인용은 검증이 아니고, 워커가 "확인함"이라 쓴 레지스트리·리포 경로는 오케스트레이터가 직접 조회해야 한다 |
| 2026-08-11 | harness | 동시 세션이 다른 세션의 진행 중 작업을 자기 커밋에 쓸어담은 **2번째 사례**(1번째는 CLAUDE.md에 기록된 `e82d8e1`): `81037c1`이 이 세션의 `openldap.yaml`·`VERSIONS.md`·설계서 §9 수정을 superset 게이트 수정과 함께 커밋. 내용은 최종본이 온전히 들어가 사고는 아니었으나, 커밋 메시지가 **실재하지 않은 "VERSIONS↔매니페스트 드리프트 해소"를 주장**해 이력이 사실과 어긋남(양쪽 다 `1.5.0`으로 일치했고 그 행에는 2.6-alpha를 실측 후 되돌린 D17 예외 근거가 이미 있었음) | CLAUDE.md의 "스테이지는 태스크가 건드린 경로만, `-A` 금지"를 재확인. 교훈: 정책이 이미 있는데 재발했다는 것은 **정책이 아니라 절차가 부족**하다는 신호 — 커밋 직전 `git status`로 예상 밖 경로가 보이면 그 자리에서 멈추고, 남의 변경이면 스테이징에서 제외한 뒤 커밋 메시지에 자기 변경만 서술한다 |
| 2026-08-11 | harness | 오케스트레이터가 `grep`/`head` 출력만 근거로 파일 내용을 단정해 **사용자에게 틀린 상태를 두 번 보고**: ① "LDAP은 아직 구현 전"(실제로는 `openldap.yaml` 148줄 + federation 215줄 + `pg_hba` 완비) ② "VERSIONS.md는 2.6.10-alpha인데 매니페스트는 1.5.0이라 드리프트"(실제로는 양쪽 다 1.5.0으로 일치, 게다가 그 행에는 2.6-alpha를 실측하고 의도적으로 되돌린 D17 예외 근거가 이미 적혀 있었음) | 편집·판단 대상 파일은 `Read`로 원문을 재확인한 뒤 결론을 낸다. 교훈: **요약 도구의 출력은 증거의 요약이지 증거가 아니다** — 특히 "없다/다르다"는 부정형 주장은 단일 grep으로 성립시키지 않는다. 부수 교훈: 기존 결정을 "결함"으로 부르기 전에 그 결정의 비고란을 먼저 읽을 것 |
| 2026-08-19 | harness | beluga-manager 정책 컴파일러 구현 중 "Trino는 Keycloak JWT의 `groups` 클레임으로 인가를 판단한다"(D-A)는 미검증 전제가 6라운드에 걸친 수정 작업 동안 유지됨 — masking·행 필터 Rego 규칙이 Trino가 절대 호출하지 않는 경로 위에 지어졌다. Trino 483 공식 문서 조사(research-trino-auth) 후에야 `input.context.identity.groups`는 group provider(file/ldap)로만 채워지고 OAuth2에는 groups 클레임 속성 자체가 없음이 드러남 | D-A 폐기, LDAP group provider(D-D)로 재설계. 교훈: 외부 시스템이 "이 클레임을 이렇게 읽을 것"이라는 전제는 구현 착수 전에 공식 문서로 1회 확정한다 — 착수 후 결함을 라운드마다 하나씩 고치는 동안에도 그 밑의 근본 전제 자체를 의심하지 않으면 라운드 비용이 계속 헛되이 쌓인다 |
| 2026-08-19 | harness | Task 11의 롤 이름 `sed` 일괄 치환(`beluga-analyst`→`analysts` 등) 이후 `validate.test.ts`의 "하이픈을 포함한 롤 이름은 허용한다" 테스트가 치환된 데이터엔 하이픈이 전혀 없는 채로 그린 상태를 유지 — 제목은 여전히 하이픈 수용을 주장하지만 실제로는 그 무엇도 증명하지 않게 됐다. 실패하지 않았기 때문에 구현자가 자진 신고한 결함 목록(6건)에도 없었고, 리뷰에서 7번째 케이스로 별도 발견됨 | 오케스트레이터가 실제 하이픈이 든 이름(`data-team` 등)으로 테스트 데이터를 되돌려 정정. 교훈: 이름 일괄 치환 후에는 잔존 여부만 grep할 게 아니라, 테스트 제목이 주장하는 속성이 치환 후 데이터에도 남아 있는지 테스트별로 대조한다 — 그린 유지가 곧 무결함이 아니다 |
| 2026-08-19 | harness | 공개 준비를 위한 히스토리 세탁(`git filter-repo --replace-text`) 전 자동 비밀 후보 추출이 10건 중 4건을 오탐(`any` — SeaweedFS S3 키 플레이스홀더, `os.environ`, `os.getenv`, `password` 단어 자체)으로 냄 — 그대로 치환했다면 정상 코드·플레이스홀더가 깨져 리포가 손상됐을 것 | 치환 전 후보 10건 전부를 오케스트레이터가 육안 대조해 오탐 4건을 제외하고 실제 비밀 8건만 치환, 치환 후 `git log -S`로 전건 0커밋을 확인. 교훈: 자동 추출 도구의 "비밀 패턴 매치"는 후보이지 확정이 아니다 — 되돌릴 수 없는 작업(히스토리 재작성)일수록 자동 산출물을 그대로 실행하지 말고 각 건을 사람이 직접 볼 것 |
| 2026-08-25 | gitops | 크로스 네임스페이스 짧은 이름 DNS 결함이 같은 작업 범위 안에서 **4번 재발**: ① Flink SQL 파일 2개(`cdc_mirroring.sql`/`events_sessionization.sql`)의 `lakekeeper`/`seaweedfs-s3` ② Flink operator 자체의 `s3.endpoint` ③ Trino의 **자기 자신의** Iceberg catalog `s3.endpoint`(#105 수정 스윕에서 빠짐) — `SHOW TABLES`처럼 메타데이터만 건드리는 쿼리는 S3를 안 거쳐 통과하고, `SELECT COUNT(*)`처럼 실제 데이터를 읽는 쿼리만 80~130초 뒤 `UnknownHostException`으로 실패해 처음엔 "리소스 압박"으로 오진단함 | K8s 짧은 이름(`seaweedfs-s3` 등)은 호출자 자신의 네임스페이스에서만 풀린다 — 네임스페이스를 넘는 참조는 항상 `<service>.<namespace>.svc.cluster.local`. 교훈: 이 결함 유형이 한 번 나오면 "같은 파일"이 아니라 **리포 전체를 grep**해서 같은 패턴의 다른 인스턴스를 찾는다. 메타데이터 전용 쿼리(`SHOW TABLES` 등)의 성공은 데이터 경로(S3/객체 스토리지) 도달성을 증명하지 않는다 — 실제 `SELECT` 실행까지 확인할 것 |
| 2026-08-25 | harness | 이 머신은 동시에 20개 이상의 다른 Claude 세션이 각자 다른 리포(narwhal, kube-ready-box, ldapium 등)에서 `kubectl`을 쓴다 — 공유 `~/.kube/config`의 `current-context`를 어느 세션이든 수시로 바꿔, `kubectl get pods`가 빈 결과나 "secret not found"를 내도 **beluga 클러스터가 아니라 엉뚱한(`kind-*` 등) 클러스터에 요청이 간 것**일 수 있다. 이 세션도 "시크릿이 사라졌다/CRD가 없다"는 1시간짜리 오진단을 겪음 | `kubectl --context=beluga config view --minify --flatten > /tmp/beluga-kubeconfig.yaml` 한 번 만들어 이후 모든 호출에 `KUBECONFIG=/tmp/beluga-kubeconfig.yaml`을 붙인다(공유 `~/.kube/config`는 건드리지 않는다). 클러스터 상태가 "갑자기 사라진 것처럼" 보이면 실제 드리프트보다 이 레이스를 먼저 의심할 것 |
| 2026-08-25 | gitops | `beluga-platform`/`beluga-data` Application이 `selfHeal: true`라, 실검증을 위해 `kubectl apply`(서버사이드 포함)로 직접 편집하면 ArgoCD가 몇 분 내로 **조용히 origin/main 상태로 되돌린다** — 에러 없이 그냥 고친 게 다시 깨진 것처럼 보여 같은 수정을 여러 번 반복하게 만듦. 별개로, ConfigMap만 바꾼 뒤 파드가 새 값을 반영하지 않는 경우도 반복됨(K8s가 ConfigMap 변경으로 파드를 자동 재시작하지 않는 것은 정상 동작인데 매번 "왜 안 바뀌지"로 재조사함). 또한 `.status.operationState.operation.sync.revision`이 오래된 훅 Job(다른 원인으로 멈춘)에 걸려 새 커밋을 반영 못 하는 채로 "OutOfSync/Running"만 반복하는 사례도 발생 — `beluga-root`(app-of-apps)가 Application을 재생성해도 안전함을 확인(cascade-delete finalizer 없음, 실배포 리소스는 그대로 유지) | 실제 반영은 커밋+푸시 먼저, 그다음 `argocd.argoproj.io/refresh=hard` 애노테이션 또는 sync로 확인 — 푸시 없는 `kubectl apply`는 확인용으로만 짧게 쓰고 곧 푸시한다. ConfigMap만 바꿨다면 관련 Deployment에 `kubectl rollout restart`를 명시적으로 호출한다. sync operation이 옛 revision에 멈춰 있으면(자기 관련 없는 훅이 원인이어도) Application 재생성으로 풀 수 있다 |
| 2026-08-25 | analytics | Trino 코디네이터가 APISIX 게이트웨이(포트 80) 경유로 응답할 때 `nextUri`(페이지네이션 후속 URL)에 **바깥에서 도달 불가능한 포트**(`:9080`, 어느 Service·포트포워드에도 없는 값)를 실어 보낸다 — Trino Service·APISIX 어디에도 이 포트는 정의돼 있지 않다(근본 원인 미확인, `http-server.process-forwarded=true`와 관련 추정). 이 값을 그대로 따라가면 라이브 쿼리 검증 스크립트가 첫 페이지 이후 전부 타임아웃/연결 실패로 죽는다 | `nextUri`를 따라가기 전에 호스트명 뒤 포트를 항상 벗겨내고(`sed -E 's#(host):[0-9]+#\1#'`) 원래 경로로만 요청한다. 부수 교훈: Trino statement 프로토콜은 여러 페이지에 걸쳐 `data`를 나눠 보내고 **마지막 페이지는 정상 성공 시에도 `data: null`**이다 — 마지막 페이지만 보고 "결과 없음"이라 판단하면 성공한 쿼리도 실패로 오진단한다. 전 페이지의 `data`를 누적해야 한다 |
| 2026-08-25 | orch | Task 16(Trino OAuth2 도입) 라이브 실측이 코디네이터에 **직접**(`https://localhost:8443`) 접근한 결과만으로 완료 판정됨 — 문서화된 유일한 사용자 진입점인 게이트웨이 경유(`http://trino.local.beluga.internal`, 포트 80)는 한 번도 찔러보지 않음. 실제로는 OAuth2가 `http-server.process-forwarded=true`와 결합해 게이트웨이의 `X-Forwarded-Proto: http`를 그대로 신뢰, 그 경로 전체를 403으로 막고 있었다 — 최종 whole-branch 리뷰가 아니었으면 "인증을 켰다"는 커밋이 사실은 유일한 진입점을 막은 채 배포돼 있었을 것 | 인증/게이트웨이 관련 변경은 **직접 접근**과 **문서화된 실제 진입점 경유** 둘 다 실측해야 완료로 인정한다 — 컴포넌트 자신에 대한 검증은 그 앞단 프록시/게이트웨이를 거치는 실제 사용자 경로를 증명하지 않는다. 교훈: "실측했다"고 쓰기 전에 어떤 경로로 실측했는지, 그 경로가 실제 문서화된 진입점과 같은지 자문할 것 |
| 2026-08-26 | lake | SeaweedFS StatefulSet에 `/data`용 PVC가 애초에 없어(OpenLDAP의 emptyDir 결함, D20과 동일 유형) `-s3.config` 반영을 위한 `rollout restart`가 파드를 교체하며 `beluga-lake`의 기존 Iceberg 데이터(orders/customers/events_enriched)를 전부 삭제함 — 실배포 실측. 원천 PostgreSQL `shop` DB는 별도 PVC라 무사했고 CDC(Debezium)→Flink 미러링 재가동으로 복구됨. 복구 과정에서 Lakekeeper의 웨어하우스 등록(구 `any`/`any` 자격증명, 부트스트랩 Job의 "이미 존재하면 스킵" 멱등 로직 때문에 새 자격증명으로 갱신되지 않음)이 별도 결함으로 드러나 웨어하우스 자체를 삭제 후 재생성해야 했다. Lakekeeper의 `/management/v1/warehouse/{id}/storage-credential` 갱신 API는 새 자격증명이 실제로 유효한데도 검증 단계에서 계속 Access Denied를 반환해 원인 규명에 실패(v0.13.1 내부 동작 미상) — 대신 "웨어하우스 삭제 후 부트스트랩 Job이 처음부터 재생성"이라는 이미 검증된 경로로 우회 | StatefulSet에 `volumeClaimTemplates`로 5Gi RWO PVC 추가(주의: 기존 StatefulSet의 `volumeClaimTemplates`는 즉시 수정 불가 — `replicas`/`template`/`updateStrategy` 등 한정된 필드만 인플레이스 변경 허용되므로 `--cascade=orphan`으로 StatefulSet만 삭제 후 재생성해야 한다). 교훈: ⑴ PVC 유무를 먼저 확인하지 않고 스토리지 컴포넌트를 재시작하지 말 것 — 이 리포에서 두 번째 재발(OpenLDAP, SeaweedFS)이니 신규/기존 StatefulSet 전수 점검이 필요하다 ⑵ "이미 존재하면 스킵"하는 멱등 부트스트랩 Job은 상위 리소스(웨어하우스)가 재생성됐을 때 하위 자격증명이 갱신되지 않는 사각지대를 만든다 — credential 갱신이 필요한 재해복구 시나리오에서는 스킵 조건에 자격증명 유효성도 포함하거나, 통째로 삭제 후 재생성하는 편이 더 예측 가능하다 |
| 2026-08-26 | harness | ArgoCD의 Sync hook(`argocd.argoproj.io/hook: Sync`, `BeforeHookCreation`)이 걸린 리소스를 오케스트레이터가 수동으로 `kubectl delete`+재적용하는 동안, 마침 그 Application에 대해 ArgoCD 자신의 sync 오퍼레이션이 `Running`(다른 훅 대기 중) 상태로 계속 살아있으면 둘이 같은 훅 리소스를 놓고 경합해 "방금 지웠는데 목록엔 없다"↔"부트스트랩 로그는 이미 존재한다고 한다"가 번갈아 나타나는 혼란스러운 패턴이 재현됨(Lakekeeper 웨어하우스 삭제 시도 중 실측) | `kubectl -n argocd get application <app> -o jsonpath='{.status.operationState.phase}'`로 진행 중인 오퍼레이션 유무를 먼저 확인한다. `Running`이면 그 오퍼레이션이 어떤 훅을 기다리는지(`.status.operationState.message`)를 보고, 그 훅이 막혀 있다면(디베지움류 pre-existing 결함 등) Application을 삭제 후 app-of-apps가 재생성하게 해 깨끗한 오퍼레이션 하나로 다시 시작한다 — 진행 중인 오퍼레이션과 병행해 같은 훅 리소스를 손대지 말 것 |
| 2026-08-26 | harness | `argocd-repo-server`가 일시적 DNS 실패(예: `lookup github.com ...: no such host`)를 겪은 뒤, 그 원인(DNS)이 이미 복구됐는데도 Application의 `.status.operationState.message`에는 옛 에러가 그대로 남아 반복 조회 시 동일하게 보임 — 실제로는 재시도가 전혀 일어나지 않는 캐시된 표시임을 직접 DNS 재조회로 확인 | `argocd.argoproj.io/refresh=hard` 애노테이션만으로는 repo-server 프로세스 내부의 실패 상태가 갱신되지 않는 경우가 있다 — `kubectl -n argocd rollout restart deployment/argocd-repo-server` 후 재시도하면 해소된다. 교훈: Application 상태 메시지가 여러 번 조회에도 똑같으면 "재시도가 실패 중"이 아니라 "재시도 자체가 안 일어나고 있다"를 의심할 것 |
| 2026-08-26 | harness | 메모리 압박이 심할 때 Deployment의 기본 RollingUpdate가 데드락에 빠짐: 새 파드가 `FailedScheduling`(Insufficient memory, 전 노드)으로 영원히 Pending인 채 구 파드는 계속 Running이라 `rollout restart`가 절대 끝나지 않음(실측: `kubectl describe pod`의 `0/4 nodes are available: 4 Insufficient memory` 반복) | 구 파드를 직접 `kubectl delete pod`로 지워 그 메모리 예약을 즉시 반환하면 새 파드가 그 자리에 스케줄된다 — 데모/개발 클러스터에서 몇십 초의 다운타임을 감수할 수 있을 때만 쓸 것(운영 트래픽이 있는 환경에서는 `maxUnavailable`을 낮추는 게 아니라 스케줄 여유 자체를 확보해야 한다) |
| 2026-08-30 | gitops | 이슈 #109(APISIX etcd emptyDir→PVC) 마이그레이션 라이브 검증 중 4중 결함 연쇄를 실측: ① 기존 Deployment(RollingUpdate·emptyDir "data")를 ArgoCD SSA로 PVC+Recreate로 바꾸려 하니 "may not specify more than 1 volume type"/"rollingUpdate forbidden" — 레거시 필드 매니저가 붙잡은 필드는 SSA merge로 못 바꾼다(Deployment 삭제 후 재생성으로 우회) ② local-path(WaitForFirstConsumer) PVC는 소비자 파드가 있어야 Bound되는데 ArgoCD는 "PVC Healthy" 대기 후 Deployment를 만들어 데드락(수동 `kubectl apply`로 Deployment를 먼저 만들어 우회) ③ 그 결과 새 etcd가 완전히 빈 상태로 뜨자, 오래 떠 있던 apisix 데이터플레인 파드가 "has no healthy etcd endpoint available"로 못박고 재연결 안 함(apisix Deployment도 rollout restart 필요 — 컨트롤러 재기동만으로는 불충분, break-glass 주석에 반영) ④ 완전히 빈 etcd(단 하나의 키도 없음)에서는 `/apisix/admin/routes` LIST 자체가 `{"message":"Key not found"}` 404를 내 apisix-ingress-controller의 init 대기 루프가 영원히 통과 못 함 — apisix가 먼저 자기 등록(id 등)을 써넣어야 풀린다 | ①②③④ 전부 "이 정확한 마이그레이션(emptyDir→PVC, Deployment 삭제·재생성 동반)"에서만 발생하는 일회성 전이 비용이지 이번 PVC 도입 자체의 결함은 아니다 — PVC가 붙은 뒤로는 일반적인 파드 재기동에서 데이터가 보존되므로 ①②④는 재발하지 않는다(③은 etcd 파드 IP가 바뀌는 재기동마다 이론상 재발 가능 — break-glass 절차에 apisix 재기동을 추가해 둠). 교훈: Deployment의 볼륨 타입을 바꾸는 GitOps 변경은 "라이브에 이미 그 리소스가 존재하는가"를 먼저 확인하고, 존재한다면 SSA merge가 실패할 수 있음을 전제로 삭제 후 재생성 경로를 계획에 포함할 것 |
| 2026-09-08 | gitops | CI `sast.yml`의 `trivy-config` 잡이 CRITICAL 2건 + HIGH 101건으로 실패(GitHub 실행 33313869597). ① KSV-0046: apisix-ingress-controller ClusterRole이 `apiGroups: ["apisix.apache.org"] resources: ["*"]`로 와일드카드 부여됨 ② KSV-0041: 같은 ClusterRole의 secrets get/list/watch(TLS 동기화용 읽기 전용, 필요한 권한)가 CRITICAL로 잡힘 ③ HIGH 다수(KSV-0014 readOnlyRootFilesystem 누락, KSV-0118 기본 securityContext, KSV-0109 analytics 네임스페이스 ConfigMap의 비밀값 유사 필드)는 실클러스터 검증 없이 일괄 수정 시 파드 크래시 위험이 있는 별개의 하드닝 부채 | D1: `resources: ["*"]`를 apache/apisix-ingress-controller 태그 1.8.0의 `samples/deploy/rbac/apisix_view_clusterrole.yaml`에 정의된 apisix.apache.org CRD 7종(각 `/status` 포함 14항목)로 명시 축소, verbs는 기존 값 유지 — `gitops/charts/beluga-platform/templates/apisix-gateway.yaml` ClusterRole `apisix-ingress-controller`. D2: KSV-0041은 TLS 동기화에 필요한 읽기 전용 권한이라 유지하고, 리포 루트 `.trivyignore.yaml`(misconfigurations 스키마, `id: KSV-0041`, `paths`로 apisix-gateway.yaml만 스코프)로 예외 처리 — trivy-action 0.35.0은 `.trivyignore`와 달리 `.trivyignore.yaml`을 자동 탐색하지 않으므로 `sast.yml`에 `trivyignores: .trivyignore.yaml` 입력을 명시해야 실제로 로드됨(로컬 `trivy config --ignorefile`로 검증). D3: HIGH 그룹은 즉시 차단하지 않고 `trivy-config` 잡 내 별도 스텝으로 `severity: HIGH`, `exit-code: 0`, `format: table` 비차단 스캔을 추가해 로그에 계속 노출(추적 이슈 #117) — CRITICAL만 게이트(`exit-code: 1`)로 유지. 교훈: RBAC 와일드카드를 좁힐 때는 컴파일된 값이 아니라 해당 컨트롤러 버전의 공식 배포 매니페스트를 1차 출처로 삼을 것 — 버전이 다르면 CRD 목록이 바뀐다 |
| 2026-09-08 | harness | 라이브 클러스터 읽기 전용 진단: ArgoCD 3개 앱 `Unknown`(controller→repo-server TLS 핸드셰이크 타임아웃), Keycloak 10일 CrashLoop(CNPG SSL `Read timed out`), superset-dashboard-import 8일 정체(`Temporary failure in name resolution`)가 **모두 별개 결함이 아니라 cilium-operator 180회 재시작·전 agent 동시 재시작(10일 전)과 시각이 일치하는 클러스터 네트워크 불안정**의 증상이었다. 또한 배포 리비전이 `273283e0`(08-30)에 고정돼 있어 로컬 미푸시 커밋(#114 Flink SQL 멱등 훅 등)의 런타임 검증은 원천적으로 불가 | 파드 재시작·매니페스트 수정 전에 `cilium-operator`/`cilium` agent 재시작 횟수와 ArgoCD `.status.conditions`부터 본다(레포 수정 대상이 아닌 environmental 결함을 코드로 고치려 들지 않기). 런타임 증거는 ArgoCD가 추적하는 리비전 기준으로만 주장한다 |
| 2026-09-08 | gitops | 이슈 #107 컷오버로 `db-roles.sql` 본문이 policyctl 생성물로 바뀌며 권한이 **의도적으로 축소**됨: `ALTER DEFAULT PRIVILEGES`·`ON ALL TABLES` 제거(미선언 테이블은 기본 거부, §10.1), engineers 시퀀스 `ALL`→`USAGE, SELECT`(`setval()` 불가), admins의 테이블 직접 `ALL` → engineers 상속 CRUD. 컴파일러 산출물엔 넣을 수 없는 항목(CONNECT, `GRANT admins TO beluga_admin`, D20 LOGIN 롤, 레거시 정리)은 Epilogue에 손으로 유지 | 새 테이블/시퀀스 권한이 필요하면 `policies/resources.yaml`에 `engine: postgres` 리소스로 선언하고 재컴파일한다(테스트 14가 본문 0-diff를 강제). 관리 작업(TRUNCATE/setval)은 `beluga_admin` 서비스 계정으로 수행 |
| 2026-09-08 | gitops | 이슈 #117(KSV-0109)로 Trino/Superset 설정을 **같은 이름의 ConfigMap → Secret**으로 바꾸자 ArgoCD SSA가 Deployment를 적용하지 못함: `spec.template.spec.volumes[*].configMap: Forbidden: may not specify more than 1 volume type`(볼륨 리스트가 name 키로 병합돼 configMap·secret 키가 공존). 그런데 prune은 먼저 실행돼 구 ConfigMap이 사라진 상태라 기존 파드는 살아 있어도 재기동·롤아웃이 전부 `FailedMount`로 멈춤(#109의 emptyDir→PVC와 같은 SSA 병합 결함 유형) | 라이브 복구는 `helm template` 산출물의 Deployment만 `kubectl replace`(전체 교체)로 적용해 해결. 예방: 볼륨 타입을 바꿀 땐 **볼륨 이름도 함께 바꾸거나**(SSA에서 새 항목 추가+구 항목 제거로 처리됨) 두 단계 커밋(Secret 추가 → 참조 전환 → ConfigMap 제거)으로 나눈다 |
| 2026-09-08 | harness | Cilium agent 일제 재시작(10일 전) 이후 **그 전에 뜬 파드들만** TLS 핸드셰이크가 무한 대기: ArgoCD controller→repo-server(gRPC TLS), Keycloak→CNPG(SSL `Read timed out`), Superset→Trino(`handshake operation timed out`). 같은 netns에서 새로 띄운 ephemeral 컨테이너의 openssl/curl은 즉시 성공했고 Cilium 정책/서비스맵/MTU·대용량 전송 모두 정상이라 "네트워크 장애"가 아니라 **오래된 파드의 소켓 상태(socket-LB/conntrack) 고착**이었다. 클라이언트든 서버든 오래된 쪽을 재시작하면 즉시 복구 | 증상이 "TLS만 타임아웃, 평문/신규 파드는 정상"이면 CNI 디버깅보다 관련 파드의 age를 먼저 보고(agent 재시작 시각 이전인지) 롤링 재시작한다. 덧: `argocd-application-controller`가 막히면 Application 상태가 갱신되지 않아 조건 메시지가 옛 것으로 남는다 |
| 2026-09-19 | gitops | ① Keycloak·APISIX에 `readOnlyRootFilesystem: true` 강제로 런타임 크래시(Keycloak은 Quarkus transformed-bytecode.jar, APISIX는 client_body_temp 쓰기 실패). ② `governance` 네임스페이스에 기본 적용된 `default-deny-all`(Ingress+Egress)로 인해 OpenMetadata 및 migration Job이 PostgreSQL(5432)과 OpenSearch(9200)에 도달하지 못해 Egress 차단으로 Connect timed out 발생. ③ Debezium `filtered` 모드는 기동 시점에 원천 테이블(orders/customers)이 DB에 존재하지 않으면(shop-seed 경합) `No table filters found` 예외로 작업 실패 | ① Keycloak 및 APISIX securityContext의 `readOnlyRootFilesystem`을 `false`로 완화. ② governance 네임스페이스에 DB/OpenSearch Egress를 명시 허용하는 NetworkPolicy(`openmetadata-egress`) 추가 및 migration 재실행. ③ shop-seed 완료 후 debezium task restart 호출. ④ SwitchHosts 아이디어를 차용하여 hosts 파일을 안전하고 멱등하게 관리하는 `scripts/hosts.sh`(`--apply`, `--on`, `--off`, `--remove`)를 개발하여 `up.sh`와 가이드에 통합함 |
| 2026-09-20 | harness | 3단 연쇄 장애(RCA, 근거: `research/evidence/2026-09.jsonl`). ① worker-3의 `/etc/netplan/50-vagrant.yaml`이 vagrant 병렬 프로비저닝 중 0바이트로 유실(mtime 09-19 23:43, 다른 노드는 정상)되어 클러스터 사설망 IP 미할당 → Cilium VXLAN 크로스노드 통신 단절(`cilium-health` worker-3 0/1) → 전역 파드 CrashLoopBackOff 연쇄. ② 네트워크 복구로 `argocd-repo-server`가 회복하며 `beluga-platform`이 자동 sync(13:45:46 UTC) — **바로 전날(2026-09-19 항목) 이미 고쳤던** `readOnlyRootFilesystem: false` 수정이 로컬에만 커밋되고 **push되지 않아** origin/main의 옛 버전(`true`)으로 selfHeal에 의해 조용히 되돌아가 Keycloak·APISIX가 동일 증상으로 재크래시(2026-08-25 항목의 selfHeal 함정 재발 인스턴스). ③ `openmetadata-migration` Job이 23시간째 Failed(2026-09-19 egress 결함의 후유증)로 방치되어 재실행된 적이 없어 OpenMetadata가 스키마 누락(`act_ge_property` 없음)으로 지속 크래시 | ① netplan 수동 재작성(`192.168.77.23/24`) + Cilium agent 파드 재시작으로 즉시 복구(약 11분). **VM 런타임에만 적용 — git 미반영이라 `vagrant reload`/재부팅 시 재발 가능**, 프로비저닝 자체의 안정성 게이트는 별도 과제. ② `git fetch`로 origin/main과의 파일 충돌 없음 확인(`git merge-tree`) 후 `git rebase origin/main` → `make lint`/`make validate` 통과 재확인 → push → `argocd.argoproj.io/refresh=hard`로 즉시 재동기화. ③ `kubectl delete job` 후 `helm template \| kubectl apply`로 재생성(ArgoCD가 삭제된 plain Job을 자동 재생성하지 않음을 실측) — 22초 만에 Complete. 교훈: (a) selfHeal 환경에서 로컬 fix의 "완료"는 **push 여부까지 확인**해야 한다 — 커밋만으로 mistakes-log에 "수정함"이라 적어도 push 안 하면 재발이 예정돼 있다(이번이 바로 그 사례). (b) 원인이 해소돼도 이미 backoffLimit 소진된 Job은 자동으로 낫지 않으므로 컴포넌트별 재시도 트리거를 명시적으로 재확인한다. (c) 서로 무관해 보이는 컴포넌트가 동시에 깨지면 개별 수정 전에 공유 인프라(CNI 노드 헬스, ArgoCD sync revision)부터 확인한다(2026-09-08 항목과 동일 원칙) |
| 2026-09-22 | gitops | macOS 호스트에서 `*.local.beluga.internal`(MetalLB VIP `192.168.77.200`)로만 HTTPS가 `No route to host`, 마스터(`192.168.77.10:6443`) 직접 접속은 정상이던 시점 기준 RCA(Codex 위임 + 라이브 재검증). `scripts/cluster/03-cni-metallb.sh`의 `L2Advertisement`(`beluga-l2`)에 `interfaces` 제한이 없어 speaker가 worker-2의 private망 NIC(`enp26s0`, vmnet12/`bridge102`)뿐 아니라 NAT NIC(`enp2s0`, 172.16.221.x/`bridge101`)에서도 VIP를 ARP 광고 → macOS가 동일 IP에 대해 두 브릿지 각각 다른 MAC의 host route를 **영구(`!`, 미만료) 항목**으로 학습해 `arp -d`로 캐시를 지워도 speaker가 계속 재광고하니 즉시 재현(`netstat -rn` 실측, 단순 stale 캐시 아님) | `kubectl patch l2advertisement beluga-l2 --type=merge -p '{"spec":{"interfaces":["enp26s0"]}}'`로 즉시 제한 + `03-cni-metallb.sh`의 매니페스트에 동일 `interfaces`를 영구 반영(재프로비저닝 재발 방지). 인터페이스명은 Vagrantfile이 전 노드에 동일 pcislotnumber(160/256)를 고정하므로 노드 전체 동일. **⚠ 원인 분석 정정(같은 날 다음 항목)**: ARP 이중 광고는 실재한 설정 오류라 이 수정은 유효하지만, 사용자가 겪은 접속 실패의 원인은 아니었다. 교훈: LoadBalancer의 L2Advertisement은 기본이 "모든 노드 인터페이스에 광고"이므로, 노드가 NAT+private 다중 NIC를 갖는 Vagrant/VMware 환경에서는 배포 시점에 반드시 `interfaces`를 명시해야 host ARP/라우팅 레이어 경합을 막는다 |
| 2026-09-22 | harness | 위 항목의 "VIP만 `No route to host`" 진단은 잘못된 대조군 위에 세워졌다. 기준선 `192.168.77.10:6443`은 비표준 포트였고, 호스트의 Avast Web Shield(네트워크 확장, HTTP/HTTPS 투명 프록시)가 **raw 소켓 클라이언트(curl/kubectl)의 80/443 흐름**을 가로채 `192.168.77.x`로의 연결을 즉시 `No route to host`로 실패시켰다(같은 IP에서 80/443 → `No route to host`, 8080/8443 → `Connection refused`로 판별, 게스트 tcpdump 패킷 0개). Avast의 `local.beluga.internal` 도메인 예외는 브라우저엔 적용되지만 raw-IP/소켓 클라이언트엔 무효. 이 오판으로 MetalLB·ARP·VMware·`vagrant reload worker-2`까지 헛돌았다. 실제 브라우저 장애는 앱 결함 2건: ① ArgoCD `server.insecure` 미설정 → APISIX가 TLS 종료 후 `:80` 평문으로 넘기면 argocd-server가 같은 URL로 307 무한 리다이렉트 ② `governance` default-deny-all 하에 `app=openmetadata` 인그레스 허용이 없어(2026-09-19 항목은 egress만 추가) APISIX 업스트림 8585가 타임아웃 | ① `argocd-cmd-params-cm`에 `server.insecure: "true"` + argocd-server 재시작(라이브), `scripts/gitops/01-argocd-bootstrap.sh`에 영구 반영(ArgoCD는 upstream install.yaml 설치라 GitOps 비관리). ② `11-openmetadata.yaml`에 `openmetadata-allow-apisix`(platform-system/app=apisix → 8585) 추가 + 라이브 적용. NodePort+SNI(Avast 우회)로 두 도메인 200 확인, `make lint`/`make validate` exit 0. **push 전이라 ArgoCD 미추적 — push 필요.** 교훈: (a) 연결 장애 대조는 **같은 포트**, 또는 **같은 IP·다른 포트**로 할 것 — 포트가 다르면 보안 제품의 L7 가로채기를 네트워크 장애로 오인한다 (b) 브라우저 증상은 브라우저 경로로 재현할 것 — curl은 보안 제품에 다르게 취급될 수 있다 (c) "패킷 0개" 같은 부정 결과는 같은 캡처로 정상 트래픽이 잡히는지 먼저 검증한 뒤에만 근거로 쓴다 (d) 일부 도메인만 실패하면 네트워크보다 백엔드별 차이(리다이렉트·NetworkPolicy)를 먼저 본다 |
| 2026-09-24 | ci | (false-green, 이슈 #107) test 14(`tests/14-policy-compiler-seam.sh`) 3/4단계는 `beluga-manager`의 `policyctl`로 `policies/`를 재컴파일해 배포된 `trino.rego`/`db-roles.sql` Generated Body/keycloak 매퍼와 0 diff인지 대조하는 라이브 드리프트 검증인데, `${REPO_ROOT}/../beluga-manager`와 npm이 있을 때만 실행되고 없으면 `log_warn` 후 그대로 통과했다. CI에는 형제 체크아웃이 없어 이 라이브 대조가 한 번도 실행되지 않은 채 `make validate`가 계속 초록이었다 | `MANAGER_DIR`을 `BELUGA_MANAGER_DIR` 환경변수로 오버라이드 가능하게 하고(로컬 기본값은 `../beluga-manager` 유지), `CI=true`(GitHub Actions 기본 제공) 또는 `REQUIRE_POLICY_SEAM_LIVE=1`일 때 디렉토리/npm 누락을 `log_warn`이 아니라 `exit 1`로 fail-closed 처리. `.github/workflows/ci.yml`의 `validate` 잡에 `dasomel/beluga-manager`를 커밋 SHA `406663a8b6946a279455ea3b6cb58b3585424970`로 고정 체크아웃(`persist-credentials: false`) + `actions/setup-node` + `npm ci`를 추가하고 `BELUGA_MANAGER_DIR`로 가리킴. `scripts/ci/check-ci-stage-parity.py`의 setup-step 예외 목록에 `actions/setup-node`/`npm ci`를 추가(기존 `azure/setup-helm`/`pip install` 예외와 동일 범주라 문서 표 갱신 불필요). 로컬 검증: `BELUGA_MANAGER_DIR`을 SHA 고정 워크트리로 지정하면 3/4단계가 실제로 실행되어 `trino.rego 0 diff`·Keycloak seam·`db-roles.sql` Generated Body 0 diff까지 통과(exit 0); `REQUIRE_POLICY_SEAM_LIVE=1`/`CI=true` + 디렉토리 없음 조합은 둘 다 `exit 1`로 즉시 실패. 교훈: "연동 가능한 환경일 때만" 실행되는 드리프트 검사는 CI에 그 환경이 실제로 존재하는지부터 확인한다 — 없으면 `log_warn` 경로가 검증을 안전망 없이 건너뛰는 CI 상시 false-green이 된다 |

