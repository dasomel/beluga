apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: postgres-main
  namespace: beluga-data
spec:
  instances: 1
  imageName: "ghcr.io/cloudnative-pg/postgresql:17.0"
  storage:
    size: 5Gi
  bootstrap:
    initdb:
      database: {{ .Values.cnpg.dbNameShop }}
      owner: beluga_admin
      secret:
        name: postgres-admin-credential
      postInitApplicationSQL:
        - "CREATE DATABASE beluga_meta;"
        - "GRANT ALL PRIVILEGES ON DATABASE beluga_meta TO beluga_admin;"
        - "CREATE DATABASE lakekeeper;"
        - "GRANT ALL PRIVILEGES ON DATABASE lakekeeper TO beluga_admin;"
        - "CREATE DATABASE keycloak;"
        - "GRANT ALL PRIVILEGES ON DATABASE keycloak TO beluga_admin;"
        - "CREATE DATABASE openmetadata;"
        - "GRANT ALL PRIVILEGES ON DATABASE openmetadata TO beluga_admin;"
  postgresql:
    parameters:
      wal_level: "logical"
      max_wal_senders: "10"
      max_replication_slots: "10"
---
apiVersion: v1
kind: Secret
metadata:
  name: postgres-admin-credential
  namespace: beluga-data
type: Opaque
stringData:
  username: beluga_admin
  password: SET-AT-BOOTSTRAP
